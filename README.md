# SMS_Microservice_CS361
Microservice for CS361 to receive a message from teammates application and send it via SMS.

Use "pip install -r requirements.txt" to install all relevant modules before running. 